# Add GMD vars to GMD server catalog

Add GMD vars to GMD server catalog

## Usage

``` r
add_gmd_vars(ctl)
```

## Arguments

- ctl:

  data.table of server catalog raw

## Value

data.table with more variables
