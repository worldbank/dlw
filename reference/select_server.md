# Select base server

Helper function to select server

Helper function to select collection argument for endpoint based on
server

## Usage

``` r
select_server(server = NULL)

select_collection(server = NULL)
```

## Arguments

- server:

  character: c("GMD"). Defaults to NULL (ie. GMD).

## Value

character
